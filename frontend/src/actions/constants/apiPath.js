export const API_DEPARTMENT_URL = `http://${SERVER_HOSTNAME}:${SERVER_PORT}/departments`;
export const API_EMPLOYEE_URL = `http://${SERVER_HOSTNAME}:${SERVER_PORT}/employees`;
export const API_LOGIN_URL = `http://${SERVER_HOSTNAME}:${SERVER_PORT}/login`;
export const API_REGISTER_URL = `http://${SERVER_HOSTNAME}:${SERVER_PORT}/register`;